name: Build and Push to ACR

on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Login to ACR
        uses: docker/login-action@v2
        with:
          registry: nataliaragacr.azurecr.io
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Build and Push Frontend
        run: |
          docker build -t nataliaragacr.azurecr.io/rag-app:v2 -f app/Dockerfile .
          docker push nataliaragacr.azurecr.io/rag-app:v2

      - name: Build and Push Backend
        run: |
          docker build -t nataliaragacr.azurecr.io/rag-faiss:v2 -f faiss/Dockerfile.faiss .
          docker push nataliaragacr.azurecr.io/rag-faiss:v2